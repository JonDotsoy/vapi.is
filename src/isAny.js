/**
 * Check a `value` if is any value.
 *
 * @param {any} value - value to evaluate
 * @param {true} result evaluation
 * @example
 * isAny('a string') // => true
 * isAny(12) // => true
 */
export const isAny = (value) => true
